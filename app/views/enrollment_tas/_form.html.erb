<%= form_for(enrollment_ta) do |f| %>
  <% if enrollment_ta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enrollment_ta.errors.count, "error") %> prohibited this enrollment_ta from being saved:</h2>

      <ul>
      <% enrollment_ta.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %>
    <%= f.number_field :course_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
