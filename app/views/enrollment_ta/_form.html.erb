<%= form_for(enrollment_tum) do |f| %>
  <% if enrollment_tum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enrollment_tum.errors.count, "error") %> prohibited this enrollment_tum from being saved:</h2>

      <ul>
      <% enrollment_tum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %>
    <%= f.number_field :course_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
